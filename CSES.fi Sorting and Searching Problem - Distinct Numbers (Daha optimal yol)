Daha ıisa ancaq əvvəlkindən 0.03 saniyə çox kod (tövsiyyə olunan).


#include <bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int n;cin>>n;
    int say = 0;
    set<int>s;
    for(int i = 0;i<n;i++)
    {
    	int x;cin>>x;
    	s.insert(x);
	}
	cout<<s.size()<<endl;
}
